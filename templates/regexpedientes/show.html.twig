{% extends 'base.html.twig' %}

{% block title %}{{ 'app.title.regexpedientes.show' | trans }}{% endblock %}

{% block body %}
	<main class="container-fluid">
                <div id="content" class="container-fluid" {{ stimulus_controller('table')}} data-table-target="content">
                        {{ include('regexpedientes/_pasosList.html.twig', { 
                        'pasosexpedientes': pasosexpedientes,
                        'pageSize' : pageSize,
                        'page': page,
                        'sortName': sortName,
                        'sortOrder': sortOrder,
                        'returnUrl': returnUrl,
                        }) }}
                </div>
	</main>
{% endblock %}