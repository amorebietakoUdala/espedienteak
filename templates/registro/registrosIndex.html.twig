{% extends 'base.html.twig' %}

{% block title %}{{ 'app.title.registros.index' | trans }}{% endblock %}

{% block body %}
	<main class="container-fluid">
        {{ include('registro/_searchForm.html.twig') }}
        <div id="content" class="container-fluid" {{ stimulus_controller('table', 
                    { 'url': path('registros_index'),
                    'exportName': 'registros',
                    'options': { 'search' : true, 'showExport' : false },
                    'filters': filters is not empty ? filters : '{}',
                })}}  data-table-target="content">
                {{ include('registro/_listRegistros.html.twig', { 
                    'registros': registros,
                    'pageSize' : pageSize,
                    'page': page,
                    'sortName': sortName,
                    'sortOrder': sortOrder,
                }) }}
        </div>
	</main>
{% endblock %}
